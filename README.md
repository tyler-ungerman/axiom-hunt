# Axiom Hunt

Copper Workshop discovery game: failed recipes → write executable axiom cards → unlock crafts.
Local-first. Deterministic sim (not LLM physics). State survives reload.

## Run

```bash
npm install
npm run dev
npm test
```

Feature branch: `forge/axiom-v0`.
